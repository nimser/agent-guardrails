id: test
name: Test
description: Test
rules:
  - id: test.invalid-regex-bash
    title: R1
    description: D1
    phase: before-tool
    match:
      type: bash-command
      pattern: '[invalid'
    defaultAction:
      type: block
      message: 'Blocked: {matched}'
