id: test
name: Test
description: Test
rules:
  - id: r1
    title: R1
    description: D1
    phase: before-tool
    match:
      type: file-path
      pattern: '(unclosed'
    defaultAction:
      type: block
      message: Blocked
