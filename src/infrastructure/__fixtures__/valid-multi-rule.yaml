id: multi-rules
name: Multi Rules
description: Multiple rules
rules:
  - id: test.rule-1
    title: Rule 1
    description: First
    phase: before-tool
    match:
      type: bash-command
      pattern: cmd1
    defaultAction:
      type: block
      message: 'Blocked 1: {matched}'
  - id: test.rule-2
    title: Rule 2
    description: Second
    phase: before-tool
    match:
      type: bash-command
      pattern: cmd2
    defaultAction:
      type: block
      message: 'Blocked 2: {matched}'
